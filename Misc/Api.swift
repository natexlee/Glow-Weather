//
//  Api.swift
//  Glowing Weather
//
//  Created by Nate Lee on 8/4/21.
//

import UIKit
/*
class currentView {
var weatherInfo: weatherData?

func fetchDataFromCity(city: String) {
    let urlstr = "http://api.weatherapi.com/v1/current.json?key=a84577bae9694076b76213117210108&q=hamilton"

    if let url = URL(string: urlstr) {
        URLSession.shared.dataTask(with: url) { (data, response, error) in
            let decoder = JSONDecoder()

            if let data = data {
                do {
                    let searchResponse = try decoder.decode(weatherData.self, from: data)
                    self.weatherInfo = searchResponse
                    
                } catch {
                    print(error)
                }
            }
        }.resume()
    }
}
    
}
*/
